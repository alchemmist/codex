name: alchemmist-release

on:
  push:
    tags:
      - "alchemmist-v*.*.*"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  build:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 90
    defaults:
      run:
        working-directory: codex-rs
    env:
      CARGO_INCREMENTAL: "0"
      CARGO_NET_GIT_FETCH_WITH_CLI: "true"
      CARGO_PROFILE_RELEASE_DEBUG: "0"
      CARGO_PROFILE_RELEASE_SPLIT_DEBUGINFO: "off"
      CARGO_TARGET_DIR: ${{ github.workspace }}/codex-rs/target
      TARGET: ${{ matrix.target }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: macOS arm64
            runner: macos-15
            target: aarch64-apple-darwin
            platform: mac
          - name: Linux x86_64
            runner: ubuntu-24.04
            target: x86_64-unknown-linux-gnu
            platform: linux

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          persist-credentials: false

      - uses: dtolnay/rust-toolchain@e081816240890017053eacbb1bdf337761dc5582
        with:
          targets: ${{ matrix.target }}

      - name: Validate release version
        shell: bash
        run: |
          set -euo pipefail
          [[ "${GITHUB_REF_NAME}" =~ ^alchemmist-v[0-9]+\.[0-9]+\.[0-9]+$ ]]
          tag_version="${GITHUB_REF_NAME#alchemmist-v}"
          cargo_version="$(sed -nE 's/^version = "([0-9]+\.[0-9]+\.[0-9]+)"$/\1/p' Cargo.toml | head -n 1)"
          [[ "${tag_version}" == "${cargo_version}" ]]

      - name: Configure rusty_v8 artifacts
        uses: ./.github/actions/setup-rusty-v8
        with:
          target: ${{ matrix.target }}

      - name: Build Codex
        shell: bash
        run: |
          export STABLE_GIT_COMMIT="${GITHUB_SHA::8}"
          cargo build --locked --release --target "${TARGET}" --bin codex

      - name: Package Codex
        shell: bash
        env:
          PLATFORM: ${{ matrix.platform }}
        run: |
          set -euo pipefail
          archive="codex-${TARGET}.tar.gz"
          staging="${RUNNER_TEMP}/codex-${TARGET}"
          mkdir -p "${staging}"
          cp "${CARGO_TARGET_DIR}/${TARGET}/release/codex" "${staging}/codex"
          strip "${staging}/codex"
          if [[ "${PLATFORM}" == "mac" ]]; then
            codesign --force --sign - "${staging}/codex"
          fi
          tar -C "${staging}" -czf "${RUNNER_TEMP}/${archive}" codex
          if [[ "${PLATFORM}" == "mac" ]]; then
            (cd "${RUNNER_TEMP}" && shasum -a 256 "${archive}" > "${archive}.sha256")
          else
            (cd "${RUNNER_TEMP}" && sha256sum "${archive}" > "${archive}.sha256")
          fi

      - name: Upload release artifact
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f
        with:
          name: release-${{ matrix.target }}
          path: |
            ${{ runner.temp }}/codex-${{ matrix.target }}.tar.gz
            ${{ runner.temp }}/codex-${{ matrix.target }}.tar.gz.sha256
          if-no-files-found: error
          retention-days: 1

  release:
    name: GitHub Release
    needs: build
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - name: Download release artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          pattern: release-*
          path: dist
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@153bb8e04406b158c6c84fc1615b65b24149a1fe
        with:
          name: ${{ github.ref_name }}
          tag_name: ${{ github.ref_name }}
          generate_release_notes: true
          make_latest: true
          fail_on_unmatched_files: true
          files: dist/*
