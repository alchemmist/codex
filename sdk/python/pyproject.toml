[build-system]
requires = ["uv_build>=0.11.19,<0.12"]
build-backend = "uv_build"

[project]
name = "antex-sdk"
version = "0.0.0-dev"
description = "Python SDK for Antex"
readme = "README.md"
requires-python = ">=3.10"
license = "Apache-2.0"
authors = [{ name = "OpenAI" }]
keywords = ["codex", "sdk", "llm", "ai", "agents"]
classifiers = [
  "Development Status :: 5 - Production/Stable",
  "Intended Audience :: Developers",
  "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = ["pydantic>=2.12", "antex-cli-bin==0.0.0-dev"]

[project.urls]
Homepage = "https://github.com/alchemmist/antex"
Repository = "https://github.com/alchemmist/antex"
Issues = "https://github.com/alchemmist/antex/issues"
Documentation = "https://github.com/alchemmist/antex/tree/main/sdk/python/docs"

[dependency-groups]
format = ["ruff>=0.15.8"]
test = [
  "openai-codex-cli-bin==0.147.0",
  "pytest>=8.0",
  "datamodel-code-generator==0.31.2",
  { include-group = "format" },
]
dev = [
  { include-group = "test" },
  { include-group = "format" },
]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.ruff]
target-version = "py310"
required-version = ">=0.15.8"
line-length = 100
extend-exclude = [
  "notebooks/**",
  "src/antex_sdk/generated/**",
]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "C4"]
ignore = ["E501"]
preview = true
extend-safe-fixes = ["ALL"]
unfixable = ["F841"]

[tool.ruff.lint.per-file-ignores]
"examples/**/*.py" = ["E402"]
"tests/test_real_app_server_integration.py" = ["E402"]

[tool.ruff.lint.isort]
combine-as-imports = true

[tool.uv]
exclude-newer = "7 days"
index-strategy = "first-index"

[tool.uv.pip]
exclude-newer = "7 days"
index-strategy = "first-index"

[tool.uv.sources]
antex-cli-bin = { path = "../python-runtime" }
