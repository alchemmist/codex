load("//:defs.bzl", "antex_rust_crate")

SCHEMA_FIXTURES = glob(
    ["schema/generated/*.json"],
    allow_empty = False,
)

antex_rust_crate(
    name = "hooks",
    compile_data = SCHEMA_FIXTURES,
    crate_name = "antex_hooks",
    integration_compile_data_extra = SCHEMA_FIXTURES,
    test_data_extra = SCHEMA_FIXTURES,
)
