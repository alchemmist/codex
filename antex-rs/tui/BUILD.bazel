load("//:defs.bzl", "MACOS_WEBRTC_RUSTC_LINK_FLAGS", "antex_rust_crate")

antex_rust_crate(
    name = "tui",
    compile_data = ["//:FORK_VERSION"] + glob([
        "assets/**",
        "frames/**",
        "src/workflow/*.py",
    ]),
    crate_name = "antex_tui",
    extra_binaries = [
        "//antex-rs/cli:antex",
    ],
    integration_compile_data_extra = ["src/test_backend.rs"],
    rustc_flags_extra = MACOS_WEBRTC_RUSTC_LINK_FLAGS,
    test_data_extra = glob([
        "src/**/*.rs",
        "src/workflow/*.py",
        "src/**/snapshots/**",
        "tests/fixtures/**",
    ]),
    test_shard_counts = {
        "tui-unit-tests": 8,
    },
    # The startup symlink test applies its own Seatbelt policy, which cannot be nested.
    test_tags = ["no-sandbox"],
)
