load("//:defs.bzl", "antex_rust_crate")

antex_rust_crate(
    name = "goal",
    compile_data = glob([
        "templates/**",
    ]),
    crate_name = "antex_goal_extension",
    integration_compile_data_extra = [
        "src/accounting.rs",
        "src/steering.rs",
    ] + glob(["templates/**"]),
)
