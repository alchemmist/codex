# Antex diagnostic and export extension

Install it with `antex extensions install diagnostics`.

- `/context` shows the bounded model-visible context.
- `/system-prompt` shows the complete built-in system prompt.
- `/dump [path]` writes the bounded session transcript to a new workspace file.

Exports never overwrite an existing file and paths cannot leave the workspace.
