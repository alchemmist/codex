# Antex plan extension

Install it with `antex extensions install plan`.

- `/todo` shows the persistent TODO panel.
- `/todo text` appends an item.
- `/todo [{"text":"task","done":true}]` replaces the bounded list.
- `/todo-clear` clears it.

The panel state follows the active session branch and never enters model context.
